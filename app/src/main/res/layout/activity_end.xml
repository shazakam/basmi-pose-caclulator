<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".EndActivity">

    <TextView
        android:id="@+id/textView"
        android:layout_width="194dp"
        android:layout_height="96dp"
        android:layout_marginEnd="108dp"
        android:layout_marginBottom="524dp"
        android:text="@string/thank_you"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <Button
        android:id="@+id/printResults"
        android:layout_width="124dp"
        android:layout_height="64dp"
        android:layout_marginStart="60dp"
        android:layout_marginBottom="84dp"
        android:onClick="onClickPrintResults"
        android:text="Print"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/startOver"
        android:layout_width="127dp"
        android:layout_height="64dp"
        android:layout_marginStart="16dp"
        android:text="Next"
        app:layout_constraintBottom_toBottomOf="@+id/printResults"
        app:layout_constraintStart_toEndOf="@+id/printResults"
        app:layout_constraintTop_toTopOf="@+id/printResults" />
</androidx.constraintlayout.widget.ConstraintLayout>